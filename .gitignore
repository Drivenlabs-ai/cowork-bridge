# Sortie de compilation Inno Setup
Output/
*.exe

# rclone bundlé : récupéré + vérifié par la CI, jamais committé
rclone-extract/
rclone-LICENSE.txt
rclone-SHA256SUMS
rclone-*.zip

# Certificats / secrets de signature (ne JAMAIS committer)
*.pfx
*.p12
*.key

# Notes de travail internes : diagnostics, plans, leçons. Le dépôt est public et ces notes
# nomment des clients et des personnes. Elles restent sur la machine.
tasks/

# Cruft OS
.DS_Store
Thumbs.db
